import { ColorKey, ColorValue } from "./design-elements.types";

export const ColorValues: Record<ColorKey, ColorValue> = {
  noir: { value: "noir", name: "Noir" },
  sky: { value: "sky", name: "Sky" },
};
